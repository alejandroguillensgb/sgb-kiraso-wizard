script.
    function init() {
        websocket = new WebSocket("ws://echo.websocket.org/");
        websocket.onopen = function() { document.getElementById("output").innerHTML += "<p>> CONNECTED</p>"; };
        websocket.onmessage = function(evt) { 
            document.getElementById("output").innerHTML += "<p style='color: blue;'>> RESPONSE: " + evt.data + "</p>"; };
        websocket.onerror = function(evt) { 
            document.getElementById("output").innerHTML += "<p style='color: red;'>> ERROR: " + evt.data + "</p>"; 
        };  
    }
    function sendMessage(message) {
        document.getElementById("output").innerHTML += "<p>> SENT: " + message + "</p>";
        websocket.send(message);
    }
    window.addEventListener("load", init, false);

div#aceEditor.ace_editor(ui-ace="{useWrapMode : true,showGutter: true,mode: 'html',theme: 'monokai',onLoad: aceLoaded,onChange: aceChanged}" ng-model="code") 

div(style="height: 250px; overflow-y: scroll; background-color: black;", scroll="messages")
    #output(ng-repeat="message in messages track by $index")
        {{ message }}
  
      

